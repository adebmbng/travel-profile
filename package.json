{
  "name": "rasuna-travel",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/prerender.mjs && node scripts/verify-static-routes.mjs",
    "preview": "vite preview",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "verify:routes": "node scripts/verify-static-routes.mjs"
  },
  "devDependencies": {
    "vite": "^7.0.0",
    "vitest": "^3.2.4"
  }
}
